# 更新日志 / Changelog

本项目所有版本变更记录。新版本发布前在文件顶部新增段落即可，
CI 发版时会自动将当前版本段落上传为 GitHub Release 发行说明。

格式约定：每个版本一个二级标题（`## v1.3.4`），保持与 git tag 同名；
段落顺序为「问题/变更 → 影响」，条目符号统一使用 `-`。

## v1.4.2

- 复盘续下：一方被将死/困毙时「当前局面续下」按钮自动禁用（浏览前后
  切换终局/非终局局面时按钮状态实时跟随），终局局面不再允许续走
- 修复：复盘续下/残局 0 步直接结束不再无视局面判和——起始局面可能
  明显失衡，现按当前摆盘局面真实分析结算；均势判和捷径仅保留给标准
  开局 0 步（不消耗引擎）
- 桌面端：Windows 与 macOS 窗口打开时在屏幕可视区域居中（此前固定
  出现在左上角附近），小屏仍自动收缩兜底
- 代码整理（行为不变）：对局控制器持久化/归档拆至 part 文件，数据类型
  拆至独立文件，结束弹窗与对局页构建方法各自成块
- 测试：新增将死禁用续走、0 步复盘续下评分结算、0 步标准对局判和
  捷径、终局/非终局导航按钮状态切换等用例

## v1.4.1

- 结束此局流程升级：点击「结束」立即按与大师档同一标准（深度 12 + 3s）
  预分析当前局势，弹窗展示当前评分与即将判定的胜负和，确认后复用预评分
  结算、不重复分析；分析失败在弹窗内透传具体原因，此时确认结束走原
  兜底路径（重新分析、失败按平局结算）
- 胜负和判定标准调整：分差 600 厘兵降为 500 厘兵（超过 500 判胜，以内
  判和；绝杀分必然判胜）
- 提示角标：建议走法的 ①② 数字圆牌改为「优」「良」文字圆牌（仍在
  箭杆中点、绿/蓝配色不变）；复盘质量角标不受影响
- 大师档：新增最短思考 1 秒（略长于困难档 0.8 秒），满强度搜索不再秒回，
  落子节奏更自然
- 复盘界面：亏损提示不再显示「（你走的）」标注，统一只显示亏损数值
- 修复：结束弹窗快速连点可能叠开两个弹窗、确认后残留弹窗挡住结算
  遮罩的隐患（加防抖）

## v1.4.0

- 新增复盘续下：复盘分析完成后，点击标题栏「当前局面续下」可从当前浏览
  到的局面选择执红/执黑继续实战对弈，无需从头开局手动摆棋
- 续下对局以续下点局面作为起始局面独立归档（标题带「（复盘）」前缀），
  悔棋基准、存档恢复、重复局面判定均以自定义起始局面为准；损坏存档
  恢复失败自动回滚，不残留半截状态
- 返回复盘自动续算：续下期间分析结果与浏览位置完整保留，返回后仅补算
  新增局面（已分析步跳过），不重复消耗引擎时间
- 终局遮罩按模式调整：续下局不提供「再来一局」，返回按钮显示「返回复盘」
  （实际回到栈下复盘分析页）；普通对局三按钮不变
- 性能：合法走法生成省去逐走法重复校验（点选响应提速约 60%）；棋盘文字
  排版与折线图标签引入静态缓存，走子动画逐帧重绘不再重复排版；音效
  播放器池 dispose 顺序修正
- 移除 PGN 导出（v1.3.7 引入）：归档页分享按钮与规则说明条目相应删除
- 测试：新增复盘续下单元测试，覆盖自定义起始局面摆盘与 AI 先行调度、
  悔棋撤光回起始局面、存档恢复与损坏回滚、归档序列化与标题前缀、
  分析基准与续跑跳过、终局遮罩按钮

## v1.3.7

- 修复：中文记谱跨纵线多兵消歧缺陷——此前消歧仅在同纵线内做前/中/后
  区分，当一方兵分布在两条纵线且每线各有两个及以上时（残局可出现），
  两条线各自产出「前兵/后兵」，棋谱上无法区分是哪条线的兵
- 现按 xqbase 规范（中国象棋电脑应用规范(二)第四节）：两条纵线上均有
  至少两个兵时，从行棋方视角按「先右后左、每条线从前到后」用一~五
  统一编号（如「一兵平五」）并省略起始纵线数字，其余纵线的兵不参与
  标记；仅一条纵线多兵（如 2+1）仍沿用前/后缀，单兵记谱不受影响
- 新增测试：红方 2+2、黑方 2+2、3+2 五兵、2+1 回归用例
- 新增 PGN 导出：归档页单局一键复制标准 PGN 文本（中文纵线记谱 +
  XQ 自定义标签，按规范 ≤80 列折行），可粘贴到东萍等象棋工具
- 新增规则说明弹窗：棋谱保留/删除/导出与复盘分析交互说明，
  归档页与复盘页共用同一入口
- 对局控制器重构：可变状态统一 setter 收口、赋值变化才通知，杜绝
  赋值后漏 notify 造成 UI 不同步；合法走法随局面缓存，点选棋子不再
  全盘重算；自动保存/归档失败如实提示；终局遮罩由状态通知统一触发，
  与动画终止条件解耦，杜绝重复弹出与误终止
- 存档瘦身：棋谱归档与续玩存档仅存 uci 序列，复盘端重放逐步重算
  记谱/吃子/局面哈希/将军标记，比信任存储的 fen 更抗数据损坏
  （旧档冗余字段兼容读取）
- 性能：棋盘加 RepaintBoundary 隔离动画逐帧重绘，shouldRepaint 改用
  Zobrist 增量哈希判定（不再每帧两次 FEN 序列化）；音效播放器池
  load 幂等，防页面重建泄漏
- 主页版本标注改为运行时经 package_info_plus 读取 pubspec 版本
  （main 注入 XiangqiApp → HomePage），删除 UI 层硬编码，发版只改
  pubspec 一处
- 工程化：启用 strict-casts / strict-inference / strict-raw-types 与
  unawaited_futures / close_sinks 等缺陷捕获型 lint；新增
  .gitattributes 统一换行符（文本入库 LF，Windows 脚本保持 CRLF）

## v1.3.6

- 字体子集化：全局字体霞鹜文楷由 24.2MB 完整版改为 1.7MB 子集版入库
  （字符集 = ASCII + 源码扫描 + GB2312 一级常用字 + 补充集，脚本内置缺字
  校验；内部家族名改写 XqKai 以符合 OFL 保留名要求），安装包体积显著缩小
- 规则升级：新增 60 回合无吃子自然限着判和（≥50 回合时预警）；重复局面
  检测改用 Zobrist 增量哈希（走子 O(1) 维护，不再逐条解析 FEN）；FEN
  补全半回合/回合计数字段，兼容旧存档恒为「0 1」的历史格式
- NNUE 权重加载优化：桌面/iOS 直读安装包内资产（零复制零峰值内存），
  Android 经平台通道流式复制到私有目录（64KB 分段，带安装指纹复用）
- Windows 窗口修复：最大化不再被交互拖拽宽度上限钳制成左侧窄窗
  （限宽改由 WM_SIZING 实现，最大化不受其约束）
- 棋盘格距按宽高较小值收缩：纵向空间不足（复盘页）时不再裁切底线棋子
  与纵线号；公告弹窗许可证按钮改上下排列防截断；棋谱页副标题小字改为
  「规则说明」弹窗入口
- game_screen 拆分：提取规则横幅/状态栏/终局遮罩纯展示组件（737 → 510 行）
- 测试与 CI：新增 BoardView Golden 基线测试（基线统一 Windows 生成）；
  CI 新增 windows-test / macos-build / ios-build 工作流 job

## v1.3.5

- 修复：Windows 窗口标题乱码（源码 UTF-8 无 BOM，非中文代码页构建环境
  按 1252/GBK 解析中文字面量；runner 构建加 `/utf-8` 强制按 UTF-8 解析）
- 桌面端竖屏适配：Windows / macOS 窗口默认竖屏比例（460x820 逻辑像素，
  与移动端竖屏设计一致），交互拖拽宽度限制在竖屏范围（约 400~520），
  高 DPI 小屏（如 150% 缩放的 1080p 笔记本）自动收进工作区
- 最大化/超宽窗口时所有页面与弹窗以 520 逻辑像素列宽居中呈现，
  两侧填宣纸底色，棋盘不再随窗口拉伸变形
- 棋谱列表支持左滑删除单局，删除前弹窗确认；存档层新增单条删除
  接口（写锁内重读定位，与并发归档互不覆盖，删除失败回滚提示）
- 修复：用户走子无动画——落子后引擎同步开始思考，「思考中」通知恰好
  紧跟动画启动并被误判为悔棋（着法数不变）而立即终止；AI 走子后无
  后续通知故幸存。现在仅着法数减少（悔棋/新局）才终止进行中的动画
- 已知限制：弹窗遮罩仅覆盖居中内容列，列外区域不变暗

## v1.3.4

- 新增 Windows 发行版：CI 自动打包内置 Pikafish 引擎（SHA256 校验 +
  UCI 握手自检）与 GPLv3 / NNUE 许可证的 zip，解压即用无需另下引擎
- 修复：CI 无根目录引擎文件时 `flutter build windows` 安装步骤失败
  （`pikafish.exe` 安装规则改为存在时才生效）
- README：Windows 平台分发方式与引擎来源说明同步更新
- Windows zip 未做代码签名：首次运行 SmartScreen 可能提示，
  点「更多信息 → 仍要运行」即可（详见 README「已知限制」）
- Windows 包实机验证与 CI 冒烟：干净目录解压运行正常，应用拉起引擎
  完成端到端走棋（引擎进程 / NNUE 权重 / 着法入库三重证据）；
  CI 新增应用本体启动冒烟步骤（进程存活 + 主窗口创建）

## v1.3.3

- 游戏存档迁移至应用文档目录 JSON 文件（原子写入 + 错误提示，
  上限 100 局普通 / 50 局收藏），旧 SharedPreferences 存档自动迁移
- UCI 着法坐标严格校验（`[a-i][0-9]×2`），非法坐标抛错而非静默容错
- 引擎可靠性工程：完整 UCI 握手就绪协议、看门狗、崩溃自动重启、
  连续 3 次启动失败快速报错；可靠性测试可在无引擎 CI 上运行
- 工程拆分：main.dart 拆为 app / home / game 模块，引擎与 UI 大文件分片
- CI：新增 Android 签名发布流水线（打 v* tag 自动构建签名 APK 并发 Release）
- 历史仓库瘦身：剥离 8 个未引用大文件，仓库体积 115.4 → 71.6 MiB
- iOS：引擎改为进程内 FFI 集成（沙盒禁止子进程），静态库方案 + 构建脚本

## v1.3.2

- 复盘模式：进入自动开始分析，逐步推进着法，无需手动按钮
- 开源声明中移除 tag 版本号，源码链接指向仓库默认分支
